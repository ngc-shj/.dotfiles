# NVIDIA Sync SSH configuration
Include "~/Library/Application Support/NVIDIA/Sync/config/ssh_config"

# Per-environment SSH configurations (gitignored for privacy)
Include ~/.ssh/conf.d/*/config

ServerAliveInterval 60

Host *
    AddKeysToAgent yes
    #UseKeychain yes

